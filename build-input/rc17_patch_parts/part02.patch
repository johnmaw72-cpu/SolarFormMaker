+                onEditClient,
+                lang
+            )
+            EditableSummary(tr(lang, "Date", "التاريخ", "بەروار"), s.date, onEditClient, lang)
+        }
+    }
+    item {
+        FormCard {
+            ReviewSectionHeader(tr(lang, "System & Inspection", "المنظومة والكشف", "سیستەم و پشکنین"), onEditSystem, lang)
+            EditableSummary(tr(lang, "System type", "نوع المنظومة", "جۆری سیستەم"), systemTypeLabel(s.systemType, lang), onEditSystem, lang)
+            EditableSummary(tr(lang, "Capacity", "السعة", "توانا"), "${s.capacity} kW", onEditSystem, lang)
+            EditableSummary(tr(lang, "Phase", "الطور", "فاز"), phaseLabel(s.phase, lang), onEditSystem, lang)
+            EditableSummary(
+                tr(lang, "Organizer", "المنظم", "ڕێکخەر"),
+                s.organizerName.ifBlank { tr(lang, "Not selected", "غير محدد", "هەڵنەبژێردراوە") },
+                onEditOrganizer,
+                lang
+            )
+        }
+    }
+    item {
+        FormCard {
+            ReviewSectionHeader(
+                tr(lang, "Required Materials", "المواد المطلوبة", "ماددە پێویستەکان"),
+                onAddItem,
+                lang,
+                add = true
+            )
+            if (s.items.isEmpty()) {
+                Text(tr(lang, "No materials added", "لم تتم إضافة مواد", "هیچ ماددەیەک زیاد نەکراوە"), color = Muted, modifier = Modifier.padding(vertical = 10.dp))
+            } else {
+                s.items.forEachIndexed { index, item ->
+                    Row(
+                        Modifier.fillMaxWidth().padding(vertical = 7.dp),
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Column(Modifier.weight(1f)) {
+                            Text(item.name, color = Navy, fontWeight = FontWeight.SemiBold, fontSize = 14.sp)
+                            Text(
+                                "${item.quantity} × ${item.unitPrice.ifBlank { "—" }} ${if (item.unitPrice.isBlank()) "" else s.currency}",
+                                color = Muted,
+                                fontSize = 12.sp
+                            )
+                        }
+                        IconButton(onClick = { onEditItem(item) }) {
+                            Icon(Icons.Outlined.Edit, tr(lang, "Edit material", "تعديل المادة", "دەستکاری ماددە"), tint = InfinityTeal)
+                        }
+                        IconButton(onClick = { onReplaceItem(item) }) {
+                            Icon(Icons.Outlined.SwapHoriz, tr(lang, "Replace material", "استبدال المادة", "گۆڕینی ماددە"), tint = Navy)
+                        }
+                        IconButton(onClick = { vm.removeItem(item.id) }) {
+                            Icon(Icons.Outlined.Delete, tr(lang, "Remove material", "حذف المادة", "سڕینەوەی ماددە"), tint = Danger)
+                        }
+                    }
+                    if (index != s.items.lastIndex) HorizontalDivider(color = Border)
+                }
+            }
+            HorizontalDivider(Modifier.padding(top = 6.dp))
+            Summary(
+                tr(lang, "Estimated Total", "المجموع التقديري", "کۆی خەمڵاندراو"),
+                "${s.estimatedTotal.stripTrailingZeros().toPlainString()} ${s.currency}"
+            )
+        }
+    }
+    item {
+        FormCard {
+            ReviewSectionHeader(tr(lang, "Load / Notes", "التحميل / الملاحظات", "بار / تێبینی"), onEditNotes, lang)
+            if (s.includeLoadNote) {
+                Text(generatedLoadNote(s, lang), fontSize = 13.sp, color = Navy, modifier = Modifier.padding(top = 8.dp))
+            } else {
+                Text(tr(lang, "Load-capacity note is disabled", "ملاحظة التحميل غير مفعلة", "تێبینی بار ناچالاکە"), color = Muted, fontSize = 12.sp, modifier = Modifier.padding(top = 8.dp))
+            }
+            if (s.customNote.isNotBlank()) {
+                Text(s.customNote, Modifier.padding(top = 10.dp), fontSize = 13.sp)
+            } else {
+                Text(tr(lang, "No custom note", "لا توجد ملاحظة مخصصة", "هیچ تێبینی تایبەت نییە"), color = Muted, fontSize = 12.sp, modifier = Modifier.padding(top = 8.dp))
+            }
+        }
+    }
+    item {
+        FormCard {
+            ReviewSectionHeader(tr(lang, "Site Photos", "صور الموقع", "وێنەکانی شوێن"), onAddPhotos, lang, add = true)
+            Text(
+                tr(lang, "${s.photos.count { it.included }} of ${s.photos.size} included in PDF", "${s.photos.count { it.included }} من ${s.photos.size} مضمنة في PDF", "${s.photos.count { it.included }} لە ${s.photos.size} لە PDF دانراون"),
+                color = Muted,
+                fontSize = 12.sp,
+                modifier = Modifier.padding(vertical = 6.dp)
+            )
+            s.photos.forEachIndexed { index, photo ->
+                Row(Modifier.fillMaxWidth().padding(vertical = 5.dp), verticalAlignment = Alignment.CenterVertically) {
+                    val bmp = remember(photo.path) { decodeThumbnail(photo.path) }
+                    if (bmp != null) Image(bmp.asImageBitmap(), null, Modifier.size(46.dp).clip(RoundedCornerShape(8.dp)), contentScale = ContentScale.Crop)
+                    else Icon(Icons.Outlined.BrokenImage, null, Modifier.size(40.dp), tint = Muted)
+                    Column(Modifier.weight(1f).padding(horizontal = 8.dp)) {
+                        Text(photo.displayName, maxLines = 1, fontSize = 12.sp)
+                        Row(verticalAlignment = Alignment.CenterVertically) {
+                            Checkbox(photo.included, { vm.togglePhoto(photo.id) })
+                            Text(tr(lang, "PDF", "PDF", "PDF"), color = Muted, fontSize = 11.sp)
+                        }
+                    }
+                    IconButton({ vm.movePhoto(photo.id, -1) }, enabled = index > 0) { Icon(Icons.Outlined.ArrowUpward, null) }
+                    IconButton({ vm.movePhoto(photo.id, 1) }, enabled = index < s.photos.lastIndex) { Icon(Icons.Outlined.ArrowDownward, null) }
+                    IconButton({ vm.removePhoto(photo.id) }) { Icon(Icons.Outlined.Delete, null, tint = Danger) }
+                }
+                if (index != s.photos.lastIndex) HorizontalDivider(color = Border)
+            }
+        }
+    }
+    item { ExportActions(s, lang) }
+    item {
+        PrimaryButton(
+            tr(lang, "Save Changes", "حفظ التعديلات", "گۆڕانکارییەکان پاشەکەوت بکە"),
+            vm::saveForm,
+            Modifier.fillMaxWidth()
+        )
+        Text(
+            tr(lang, "Use the pencil and + buttons above to edit this form directly.", "استخدم أزرار القلم و + أعلاه لتعديل الاستمارة مباشرة.", "بۆ دەستکاری ڕاستەوخۆ دوگمەکانی قەڵەم و + لە سەرەوە بەکاربهێنە."),
+            color = Muted,
+            fontSize = 11.sp,
+            textAlign = TextAlign.Center,
