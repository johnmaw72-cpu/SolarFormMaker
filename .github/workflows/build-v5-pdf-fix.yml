name: Build v5 PDF Fix APK

on:
  push:
    branches:
      - chatgpt-v5-pdf-fix-build
    paths:
      - '.github/workflows/build-v5-pdf-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reconstruct v5 PDF-fixed source
        shell: bash
        run: |
          cat build-input/v5_pdf_fix/source.b64.part* | base64 -d > /tmp/igp-source.zip
          mkdir -p /tmp/igp-build
          unzip -q /tmp/igp-source.zip -d /tmp/igp-build || true
          cp build-input/v5_pdf_fix/Relations.kt /tmp/igp-build/IGP/app/src/java/com/infinitygreenpower/organizerform/data/db/Relations.kt
          test -f /tmp/igp-build/IGP/app/build.gradle.kts
          test -f /tmp/igp-build/IGP/app/src/java/com/infinitygreenpower/organizerform/export/pdf/PdfExporter.kt

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 36
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 9.4.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4.1'

      - name: Build debug APK
        id: build
        working-directory: /tmp/igp-build/IGP
        shell: bash
        continue-on-error: true
        run: |
          set +e
          gradle --no-daemon clean assembleDebug > /tmp/igp-build.log 2>&1
          STATUS=$?
          grep -E "(^e:| error:|FAILURE:|What went wrong|PdfExporter\.kt)" /tmp/igp-build.log | tail -n 120 > /tmp/igp-errors.txt || true
          cat /tmp/igp-errors.txt
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          exit $STATUS

      - name: Publish diagnostics if build failed
        if: steps.build.outcome == 'failure'
        shell: bash
        run: |
          mkdir -p build-output
          cp /tmp/igp-errors.txt build-output/v5-build-errors.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add build-output/v5-build-errors.txt
          git commit -m "Record v5 PDF-fix build diagnostics" || true
          git push || true

      - name: Stop on build failure
        if: steps.build.outcome == 'failure'
        shell: bash
        run: exit 1

      - name: Verify and rename APK
        if: steps.build.outcome == 'success'
        shell: bash
        run: |
          APK=/tmp/igp-build/IGP/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          cp "$APK" IGP-Organizer-Form-5.0.0-rc1-PDF-fix-test.apk
          sha256sum IGP-Organizer-Form-5.0.0-rc1-PDF-fix-test.apk

      - name: Upload APK artifact
        if: steps.build.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: IGP-Organizer-Form-5.0.0-rc1-PDF-fix-test-APK
          path: IGP-Organizer-Form-5.0.0-rc1-PDF-fix-test.apk
          if-no-files-found: error
          retention-days: 7

      - name: Publish APK to build branch
        if: steps.build.outcome == 'success'
        shell: bash
        run: |
          mkdir -p build-output
          cp IGP-Organizer-Form-5.0.0-rc1-PDF-fix-test.apk build-output/
          rm -f build-output/v5-build-errors.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add build-output
          git commit -m "Build v5 PDF-fix test APK" || exit 0
          git push
